services:
  react-prod:
    build:
      context: ./
      dockerfile: Dockerfile.prod
    image: docker-reactjs-sample
    ports:
      - "8080:8080"

  react-dev:
    build:
      context: ./
      dockerfile: Dockerfile.dev
    ports:
      - "5173:5173"
    develop:
      watch:
        - action: sync
          path: .
          target: /Peerprep-app